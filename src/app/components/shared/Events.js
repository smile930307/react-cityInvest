/* eslint-disable no-unused-vars */
import React from 'react';
import Marquee from "react-fast-marquee";

export const Events = () => {

  return (
    <Marquee className='fixed-bottom' gradient={false}>
      You can't handle the truth! In order to defend scrolling text, we must understand the arguments against it, and develop countermeasures. The main complaints against it are:
    </Marquee>
  );
}

export default Events;